namespace RetinaNetworking.Server
{
    class ServerConstants
    {
        public const int TICKS_PER_SEC = 30;
        public const int S_PER_TICK = 1 / TICKS_PER_SEC;
        public const int MS_PER_TICK = 1000 / TICKS_PER_SEC;
    }
}
